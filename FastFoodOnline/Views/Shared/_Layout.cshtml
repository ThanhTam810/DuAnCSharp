<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Amoca Ngọt Ngào</title>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet" />

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        :root {
            /* Nền chính và text chính */
            --bg-light: #FFEFF6; /* hồng sữa nhẹ nhàng */
            --text-primary: #4A4A4A; /* xám đậm, dễ đọc hơn nâu */
            /* Navbar & footer */
            --nav-bg: #FFB6C1; /* hồng pastel */
            --nav-text: #fff; /* chữ trắng */
            /* Màu chủ đạo (buttons, header card) */
            --clr-primary: #FF82A9; /* hồng pastel xinh */
            /* Màu nhấn (hover, accent) */
            --clr-accent: #FFAACF; /* hồng nhạt cho hover */

            --radius: 1rem;
            --transition: 0.3s ease;
            --shadow-soft: 0 5px 20px rgba(255, 130, 169, 0.25);
        }



        /* Toàn trang */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--bg-light);
            color: var(--text-primary);
            line-height: 1.6;
        }

        a {
            text-decoration: none;
            transition: var(--transition);
        }

            a:hover {
                opacity: 0.8;
            }

        /* Navbar */
        .navbar {
            background: linear-gradient(to right, #ff9ebb, #f8a5c2); /* hồng pastel giống footer */
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
        }

        .navbar-brand {
            color: #fff !important;
            font-weight: 600;
        }

            .navbar-brand:hover {
                color: #FFD9E8 !important; /* trắng → hồng nhạt khi hover */
            }

        .nav-link {
            color: #fff !important;
            margin-right: 1rem;
            font-weight: 500;
            transition: 0.3s;
        }

            .nav-link.active,
            .nav-link:hover {
                color: #ffe6f2 !important; /* pastel nhạt khi hover */
            }


        /* Card quản lý */
        .card-mgmt {
            background-color: #fff;
            border-radius: var(--radius);
            box-shadow: var(--shadow-soft);
            overflow: hidden;
            transition: transform var(--transition), box-shadow var(--transition);
        }

            .card-mgmt:hover {
                transform: translateY(-5px);
                box-shadow: 0 10px 25px rgba(0,0,0,0.12);
            }

            .card-mgmt .card-header {
                background-color: var(--clr-primary);
                color: #fff;
                font-weight: 600;
            }

            .card-mgmt .card-body {
                padding: 1.25rem;
                color: var(--text-primary) !important;
            }

            .card-mgmt .text-muted {
                color: #666 !important;
            }

        /* Button quản lý */
        .btn-mgmt {
            background: linear-gradient(90deg, #FF82A9, #FFAACF);
            color: white;
            border-radius: var(--radius);
            box-shadow: var(--shadow-soft);
        }

            .btn-mgmt:hover {
                background: linear-gradient(90deg, #FF6F91, #FFB6C1);
            }
        /* Footer */
        .site-footer {
            background-color: #FFB6C1; /* hồng pastel */
            color: #fff; /* chữ trắng */
            padding-top: 2rem;
            padding-bottom: 1rem;
            box-shadow: 0 -3px 10px rgba(0,0,0,0.05);
        }

            .site-footer a {
                color: #fff;
                font-weight: 500;
            }

                .site-footer a:hover {
                    color: #4A4A4A; /* xám đậm, dễ nhìn */
                }

        /* Social icon */
        .footer-social a {
            display: inline-block;
            margin-right: 0.75rem;
            color: #fff;
            font-size: 1.5rem;
            transition: var(--transition);
        }

            .footer-social a:hover {
                color: #FFD9E8; /* hồng nhạt khi hover */
                transform: translateY(-3px);
            }

    </style>
</head>
<body class="d-flex flex-column min-vh-100">

    @* Navbar chung *@
    @await Html.PartialAsync("_NavBar")

    @* Nội dung chính *@
    <main class="container flex-fill py-4">
        @RenderBody()
    </main>

    @* Footer chung *@
    <footer class="site-footer pt-5 pb-3 mt-auto">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-3">
                    <h5>Amoca</h5>
                    <p> Amoca – Ngọt ngào mỗi ngày, tiện lợi mọi lúc..</p>
                </div>
                <div class="col-md-4 mb-3">
                    <h6>Liên kết nhanh</h6>
                    <ul class="list-unstyled">
                        <li><a asp-controller="Home" asp-action="Index">Trang chủ</a></li>
                        <li><a asp-controller="FoodItem" asp-action="Index">Thực đơn</a></li>
                        <li><a asp-controller="Account" asp-action="Login">Đăng nhập</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-3">
                    <h6>Kết nối với chúng tôi</h6>
                    <div class="footer-social">
                        <a href="#"><i class="fab fa-facebook-square"></i></a>
                        <a href="#"><i class="fab fa-instagram-square"></i></a>
                        <a href="#"><i class="fab fa-youtube-square"></i></a>
                    </div>
                </div>
            </div>
            <hr class="border-light" />
            <div class="text-center small">
                © @DateTime.Now.Year Amoca Ngọt Ngào. Bản quyền thuộc về Amoca Ngọt Ngào.
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>